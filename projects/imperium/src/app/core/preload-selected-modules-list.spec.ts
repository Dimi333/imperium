import { PreloadSelectedModulesList } from './preload-selected-modules-list';

describe('PreloadSelectedModulesList', () => {
  it('should create an instance', () => {
    expect(new PreloadSelectedModulesList()).toBeTruthy();
  });
});
